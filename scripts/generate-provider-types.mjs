/**
 * Placeholder for future provider type generation.
 *
 * v0.1 ships curated event maps in packages/*/src/index.ts.
 * When expanding coverage, generate committed files under
 * packages/<provider>/src/generated/ and keep runtime deps at zero.
 *
 * Usage (future):
 *   pnpm generate:types
 */
console.log("No generated provider types yet. See docs/guides/generated-types.md")
